import React from 'react'
import { Box, Typography } from '@material-ui/core'
import './style.scss'

export const TopImages = () => (
  <Box id="back-to-top-anchor" className="top_images">
    <Typography>Remodelação e Reabilitação</Typography>
  </Box>
)
